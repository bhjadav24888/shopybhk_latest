{% layout 'theme' %}
<!DOCTYPE html>
<html>
<head>
  <title>{{ page.title }}</title>
</head>
<body>
  <h1>{{ page.title }}</h1>
  <p>{{ page.metafields.namespace.key }}</p>
  <span>{{ page.metafields.custom.short_text }}</span>
  <div>
    {{ page.content }}
  </div>
</body>